@using TradingResearchEngine.Application.Research

<MudPaper Outlined="true" Class="pa-4">
    <MudText Typo="Typo.subtitle2" Class="text-muted mb-3">RESEARCH PROGRESS</MudText>

    <MudStack Spacing="1">
        @CheckItem(Checklist.InitialBacktest, "Initial backtest completed")
        @CheckItem(Checklist.MonteCarloRobustness, "Monte Carlo robustness")
        @CheckItem(Checklist.WalkForwardValidation, "Walk-forward validation")
        @CheckItem(Checklist.RegimeSensitivity, "Regime sensitivity checked")
        @CheckItem(Checklist.RealismImpact, "Execution realism impact measured")
        @CheckItem(Checklist.ParameterSurface, "Parameter surface mapped")
        @CheckItem(Checklist.FinalHeldOutTest, "Final held-out test")
        @CheckItem(Checklist.PropFirmEvaluation, "Prop firm evaluation")
        @CheckItem(Checklist.CpcvDone, "CPCV overfitting assessment")
    </MudStack>

    <MudDivider Class="my-3" />

    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
        <MudText Typo="Typo.body2" Class="text-muted">Confidence Level:</MudText>
        <MudChip T="string" Size="MudBlazor.Size.Small"
                 Color="@ConfidenceColor"
                 aria-label="@($"Confidence level: {Checklist.ConfidenceLevel}")">
            @Checklist.ConfidenceLevel (@Checklist.PassedCount of @Checklist.TotalChecks)
        </MudChip>
    </MudStack>
</MudPaper>

@code {
    [Parameter, EditorRequired]
    public TradingResearchEngine.Application.Research.ResearchChecklist Checklist { get; set; } = default!;

    private Color ConfidenceColor => Checklist.ConfidenceLevel switch
    {
        "HIGH" => Color.Success,
        "MEDIUM" => Color.Warning,
        _ => Color.Error
    };

    private RenderFragment CheckItem(bool Done, string Label) => __builder =>
    {
        <MudStack Row="true" Spacing="1" AlignItems="AlignItems.Center">
            <MudText Typo="Typo.body2">@(Done ? "✅" : "⬜")</MudText>
            <MudText Typo="Typo.body2" Class="@(Done ? "" : "text-muted")">@Label</MudText>
        </MudStack>;
    };
}
